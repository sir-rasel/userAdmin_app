import { ItalicStyleDirective } from './italic-style.directive';

describe('ItalicStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicStyleDirective();
    expect(directive).toBeTruthy();
  });
});
